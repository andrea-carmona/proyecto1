import { createTheme } from '@mui/material/styles';
import {error} from 'console';
export const theme = createTheme ({
    palette: {
        primary: {
          main: "#ffffff",
        },
        secondary: {
            main: "#252424",
        },
        error: {
            main: "#D32F2F",
        },
    },

  })